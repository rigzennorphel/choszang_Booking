/**
 * Brand theme colors — highlight (cab gold) is the primary theme accent.
 * Used by tailwind.config.ts, app/layout.tsx viewport, and globals.css.
 */
export const themeColors = {
  highlight: "#f5b301",
  highlightDark: "#d99a00",
  highlightLight: "#ffd54a",
  background: "#0a0e17",
} as const;
